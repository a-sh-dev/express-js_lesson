let object = {
  name: 'hello',
  id: 5,
};
const [hello, hello1] = object;
const { obj1, obj2 } = object2;
let string = 'what should i do?';

export default function test() {
  return (
    <div
      classname="hello"
      onChange={trythis}
      type="apaya"
      alt="yesbro"
      context="yep"
    >
      <p>hello there</p>
    </div>
  );
}
